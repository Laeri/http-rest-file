use crate::model;

pub struct Serializer {}

impl Serializer {
/*     pub fn serialize(requests: Vec<model::Request>) -> String {
        let mut result = String::new();
        for &request in requests.iter() {
            result.push_str(&Serializer::serialize_request(&request));
            result.push('\n');
        }
        result
    }

    pub fn serialize_request(request: &model::Request) -> String {
        let mut result = String::new();
        todo!()
        //request.comments.iter().map(|comment| comment.to_string()).collect::Vec<String>(join("\n"))
    } */
}
